

/*// Send message to everyone on the users group
everyone.now.distributeMessage = function(message){
    //console.log('Received message from '+this.now.name +' in serverroom '+this.now.serverRoom);
    var group = nowjs.getGroup(userObj.teamID+"/"+fname);
    group.now.receiveMessage(this.now.name, message);
};

// Tell the app to add a new tab fot the given file (that was oppened)
everyone.now.addFileToChat = function(fname){
    /*console.log('Adding file chat for the user '+this.now.name + '  file: ' + fname);
    this.now.c_addFileToChat(fname);
};
*/
